# かんじラボ(github.io)改善施策 — 2026-07-10 ソースレビュー

対象: ドリル(`js/kanji-app.js` 987行)、漢字の塔(`kanji-td/`)、りかラボ(`rika/`)、算数クロスワード(`number-puzzle/`)、トップページ、tools。
**注**: 2026-07-06のコードレビュー(REPORT_CODE_REVIEW_20260706.md)の最優先指摘(TDクイズの同一選択肢判定バグ)は修正済みであることを確認した(`quiz.js` に重複選択肢の作り直し処理と意味重複フィルタが入っている)。エンゲージメント施策もPLAN_DRILL_ENGAGEMENT.mdのP1〜P10/T10〜T15が実装済み。本レビューはそれらと重複しない観点に絞る。

## メンテナンス観点

### M1. 「夢中化スタック」が3箇所に複製実装されている【最重要】
デイリー目標・連続日数・XP/称号・バッジ・時間計測・にがてとっくんの仕組みが、ドリル(`kanji-app.js`)・りか(`rika-app.js`、コメントに「かんじラボで実証済みのスタックを搭載」=移植)・漢字の塔(`kanji-td/js/core/save.js` ほか)にそれぞれ独立実装されている。仕様変更(例: ストリークの判定ルール変更)やバグ修正が常に3箇所の同期修正になり、既に将来の乖離が始まる構造。
**施策**: 共通部分を `js/engagement.js`(日付キー・ストリーク・XP・バッジ判定・時間計測)として抽出し、各アプリは`<script>`で読み込んで自アプリのキー名を渡すだけにする。ビルドなし方針のまま実現できる。今後コンテンツが増えるたび効果が複利で効く。

### M2. 進捗データのバックアップ/引っ越し手段がない
全進捗がlocalStorage(`kanji_time_v1` / `rika_state_v1` / TDのセーブ等、キーもバラバラ)にあり、端末変更・ブラウザのデータ削除で**子どもの学習記録が全消失**する。積み上げ型の設計(マスター・バッジ・ストリーク)だからこそ、消えたときの喪失感が大きい。
**施策**: 全キーをまとめてJSONでエクスポート/インポートする「ひきつぎ」画面を追加する(ファイルダウンロード+貼り付け復元)。M1の共通化と同時にやると安い。

### M3. grade1〜6.htmlのボイラープレート重複
6ファイルがほぼ同一構造(約119行×6)で、UI変更のたびに6ファイル修正になる。
**施策**: `drill.html?grade=N` の1ファイルに統合する(データは既に `data-gradeN.js` に分離済みなので、クエリパラメータで読み込むスクリプトを切り替えるだけ)。既存URLはリダイレクト用の薄いHTMLを残せば互換も保てる。

### M4. データ検証ツールがCIに組み込まれていない
`tools/check-grade-data.mjs`(全学年検証済み)という良い資産があるのに、手動実行頼み。データ修正時の実行忘れが起きうる。
**施策**: GitHub Actionsでpush時に `node tools/check-grade-data.mjs` を全学年実行するワークフローを追加する(数分で書ける)。

### M5. 計画・レポート文書がルートに散乱
PLAN_*.md×4 + DESIGN_RIKA.md + REPORT_*.md がリポジトリ直下にあり、どれが生きている文書か初見で分からない。
**施策**: `docs/` に移し、READMEに「現行の管理表はどれか」の索引を書く。

## ユーザビリティ観点

### U1. トップページに「きょうやること」の導線がない
各アプリ内には「きょうの10もん」等の強力な開始導線があるが、トップ(`index.html`)は静的なリンク集で、子どもは毎回「どれをやるか」の選択から始まる。
**施策**: トップに各アプリのデイリー達成状況(ドリル◯/りか◯/等)と「きょうの おすすめ」ボタンを置く。localStorageは同一オリジンで共有されるので、各アプリのデイリー状態をトップから読むだけで実装できる。

### U2. オフライン・ホーム画面対応がない
通信が不安定な環境(移動中等)では開けない。子どもの利用形態を考えるとPWA化の価値が高い。
**施策**: manifest.json + 最小のService Worker(静的アセットのキャッシュ)を追加し、「ホーム画面に追加」で全画面起動できるようにする。かんじサバイバー(wasm 35MB)はキャッシュ対象から除外して軽く保つ。

## 夢中になれる観点

### E1. サイト横断の「かんじラボ レベル」がない
ドリル・りか・TD・サバイバー・パズルがそれぞれ独立のXP/バッジを持ち、頑張りがコンテンツごとに分断されている。「今日はどれで遊んでもぜんぶ同じ成長につながる」という上位のメタ進捗があると、コンテンツ間の回遊が生まれ、飽きたときの離脱が「別アプリへの移動」に変わる。
**施策**: M1の共通化とセットで、全アプリのXPを合算した「ラボレベル」と共通称号をトップページに表示する。各アプリからは「+XP」を共通キーに書き込むだけ。

### E2. number-puzzleだけ夢中化スタック未搭載
昨日追加した算数クロスワードには記録の永続化すらない(詳細は `number-puzzle` リポジトリの REVIEW_20260710.md 参照)。せっかくの共通スタックの実証があるサイトなのに、新入りだけ素朴なまま。
**施策**: M1の共通ライブラリ化ができれば、number-puzzleへの搭載は「読み込んで呼ぶだけ」になる。実装順として「M1 → number-puzzleを最初の利用者にする」が良い試金石になる。

### E3. 保護者向けの見守りビューがない
勉強時間・にがて・マスター状況のデータは豊富に貯まっているが、それを俯瞰する画面がない。「今週なにをどれだけやったか」「にがてが減っているか」が見えると、声かけ(外発的動機づけ)の質が上がる。
**施策**: 集計専用の `parents.html` を1枚追加する(既存localStorageを読むだけの読み取り専用ページ。認証不要、書き込みなし)。

## 優先順位(推奨)

1. M2(進捗バックアップ) — 事故が起きてからでは遅い
2. M1(夢中化スタック共通化) — 以後の全改善の土台
3. E1+E2(ラボレベル+number-puzzle搭載) — M1の直接の果実
4. U1(トップの導線)、M4(データ検証CI)
5. U2(PWA)、E3(見守りビュー)、M3・M5
