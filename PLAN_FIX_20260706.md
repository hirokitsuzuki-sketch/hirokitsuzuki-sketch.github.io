# 修正計画 — サバイバー後半クラッシュ / TDカード補完 / 画面フィット

作成日: 2026-07-06
対象: `godot`（漢字サバイバー）+ `hirokitsuzuki-sketch.github.io`（漢字の塔）

## 1. 漢字サバイバー: 7分以降の強化取りすぎによるクラッシュ

### 原因分析
前回の対策（perf_tuner による敵数・エフェクト数制限）は「出る側」の制限だったが、
今回の報告は「プレイヤーが強くなりすぎる側」が原因:
- **攻撃速度に上限がない**: 強化「こうげきスピード」+8% とコンボ報酬 +10% が無限に積める
  → クールダウンが際限なく縮み、毎フレーム大量の弾・エフェクト・ヒット処理・SFXが発生
- 同様に移動速度・マグネット範囲・クリティカル率・経験値倍率も無限に積める
  → マグネット肥大でジェム回収が爆発し、XP洪水 → レベルアップ連打 → さらに強化が積まれる悪循環

### 対策
| 項目 | 上限 | 理由 |
|---|---|---|
| 攻撃速度倍率 (attack_speed_mult) | 2.5倍 | 連射数の直接の原因。フィーバー込みでも 3.75倍まで |
| 武器クールダウン下限 | 0.12秒 | どれだけ強化しても毎秒8発/武器まで |
| 移動速度 (move_speed) | 420 | テレポート圏との整合 |
| マグネット範囲 (magnet_radius) | 360 | ジェム回収の爆発防止 |
| クリティカル率 (crit_chance) | 60% | ダメージ数字の肥大防止（実益もほぼ頭打ち） |
| 経験値倍率 (xp_mult) | 3.0倍 | XP洪水の抑制 |
| 攻撃力 (attack_mult) | 上限なし | 数値が大きくなるだけで負荷に直結しないため |

- 上限に達したステータスは**強化候補から除外**する（無意味な選択肢を出さない）
- 全部が上限に達して候補が空になったら、強化パネルを出さずに**コイン+10のボーナス**に置き換えて
  ゲームを続行（候補0個でパネルを開くとソフトロックするため。この保護も追加）

## 2. 漢字の塔（TD）: 2〜4年の漢字カードが部分的（66〜70字）

- ドリル用マスターデータ（`js/data-grade{2,3,4}.js`）は前回の修繕で**全字カバー済み**なので、
  これを源泉に不足分を機械生成できる
- `tools/generate-grade-data.mjs` を拡張:
  既存 `QDATA["2".."4"].kanji` にある字を除いた不足分を、マスターデータから
  `{k, y, m, b:null, s:null}` 形式で `questions-data-extra.js` に `push` する形で追記生成
- 期待結果: 2年=160字 / 3年=200字 / 4年=202字（読み・漢字・意味あて問題と図鑑が全字対応に）
- 既存の手書きカード（部首・画数つき）はそのまま生かす

## 3. 漢字サバイバー: トップ画面・ゲームオーバー画面を画面いっぱいに

- 現状、背景装飾（光の玉・漂う漢字）が 1280×720 の固定座標で置かれており、
  大きな画面や縦長のスマホでは**端に装飾のない余白**が出て「小さく表示されている」印象になる
- 対策: 装飾類をビューポートの実サイズから計算して全面に散らす
  - `theme_fx.gd` の `floating_glyphs` / 新設 `glow_circles` をビューポートサイズ基準に
  - `main_menu.gd` の光の玉・透かしをビューポート全面に配置、リサイズ時に再配置
  - `result_screen.gd` も同様（勝敗の透かし文字を全面に）
- 中央のパネル/ボタン群は従来どおり中央寄せ（stretch=canvas_items + aspect=expand は設定済みで、
  キャンバス自体はブラウザ全面に広がっている）

## 追記（2026-07-06）: 1ラン10分化と展開の再調整

報告: パーフェクトに進めると開始5分でキャラ完成、7分以降はクラッシュ寸前・iPhone 15 Proでも発熱。
→ **1ステージ = 10分** とし、その中で展開が完結するよう調整する。

| 項目 | 旧 | 新 | ねらい |
|---|---|---|---|
| ボス出現 | 5:00 / 10:00 / 15:00 | **3:00 / 6:30 / 10:00** | 10分でクリアが完結 |
| 敵の解禁間隔 | 50秒ごと | 40秒ごと | 序盤のテンポ維持 |
| 敵HPの時間倍率 | +12%/分 | **+22%/分（ボスは対象外）** | キャラ完成後も歯ごたえを維持 |
| ボスHP | 時間倍率が乗っていた | ステージ倍率のみ | 10分ランで倒しきれる強さに |
| XP必要量 | 5+3L+0.15L² | **4+3L+0.35L²** | 後半のレベルアップ連打を抑制（ビルド完成を〜8分に） |
| レベルアップの持ち越し | 無制限 | 最大5 | クイズ連打スタック防止 |
| 弾の同時数 | 一律400 | **段階別 400/240/140** | 発熱・負荷の直接削減 |
| perf 低/中段階の上限 | 敵100/160ほか | さらに引き下げ（敵80/140ほか） | スマホの発熱対策 |
| XPジェム | 個別に散らばる | 近く(28px)のジェムに自動合流 | 描画・回収処理の削減 |



1. ✅ この計画を保存
2. サバイバー: game.gd にステータス上限 + 候補除外 + 空候補ガード、weapon_system.gd にCD下限
3. TD: 生成スクリプト拡張 → 再生成 → 字数を機械検証
4. サバイバー: theme_fx / main_menu / result_screen をビューポート基準に
5. headless 検証（--autostart --all-weapons 長時間）→ Web再ビルド → kanji-survivor/ 配置
6. 両リポジトリ コミット & push

## 検証

- サバイバー: 全武器進化+ステータス上限到達後も強化フローが止まらないこと（空候補ガード）、
  headless長時間でエラーゼロ
- TD: node で QDATA 全学年の kanji 数 = 160/200/202 を確認
- 画面: ブラウザでメニュー/リザルトの装飾が全面に出ることを目視
