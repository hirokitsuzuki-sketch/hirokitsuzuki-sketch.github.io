# コードレビュー・改善レポート

作成日: 2026-07-06
対象: `hirokitsuzuki-sketch.github.io`（漢字ドリル grade1〜6 / 漢字の塔 kanji-td / トップページ / ainews / tools）
対象外: 漢字サバイバー（`godot` リポジトリ・`kanji-survivor/` 配置成果物）— 別プロジェクトで改善中のため

検証方法: 全 JS/HTML の目視レビューに加え、`tools/check-grade-data.mjs` の全学年実行（全て OK）と、
クイズ生成ロジックの Node によるシミュレーション（20万問生成して選択肢の重複・曖昧性を計測）を実施。

---

## 総評

全体として素直で読みやすいコードベース。データ整合性チェックツールが既にあり、ドリルの
マスターデータは全学年で配当漢字を完全カバーしている（機械検証済み）。改善計画
（PLAN_IMPROVEMENTS.md / PLAN_FIX_20260706.md）とも方向性は一致している。

一方で、**漢字の塔のクイズ判定に「正しく答えたのに不正解になる」パターンが実在**する。
学習アプリとして子どもの信頼を損なう種類の不具合なので、最優先での修正を推奨する。

---

## 1. 【最優先】漢字の塔: クイズの正解判定の不具合

### 1-1. 同一テキストの選択肢が出ると、押す場所によって不正解になる

`kanji-td/js/core/quiz.js:192-200` の `pack()`:

```js
const choices = this.shuffle([correct, ...wrongs.slice(0, 3)]);
return { ..., correctIdx: choices.indexOf(correct), ... };
```

`indexOf` は最初に一致した位置を返すため、誤答に正解と同じ文字列が混ざると、
**見た目がまったく同じ2つのボタンのうち片方だけが正解扱い**になる。

混入経路は「いみ」問題（`quiz.js:104-117`）。誤答のフィルタが `o.k !== e.k`（漢字が違うこと）
だけで、**意味テキストの重複を除外していない**。実データには意味の重複がある:

| 学年 | 重複する意味の例 |
|---|---|
| 3年 | 「決めること」 |
| 5年 | 「まもること」「しらべること」「よいと みとめること」「きまり」など5組 |
| 6年 | 「自分のこと」 |

シミュレーション実測: 全タイプ混合で 20,000問中 24問（約0.1%）、いみ問題単独で 0.03% に発生。
確率は低いが、当たった子には「正しく押したのにバツ」になる。

**修正案（2点セット）:**
1. いみ問題の誤答フィルタに意味の一致除外を追加: `o.k !== e.k && o.m !== e.m`
2. 保険として `pack()` で `new Set(choices).size < 4` なら問題を作り直す（`build()` が null を
   返せば `generate()` のリトライループが既に面倒を見てくれる構造になっている）

なお「いみ→漢字」方向（`quiz.js:107-110`）も同様に、同じ意味を持つ別の漢字が誤答に並ぶと
**実質正解が2つある問題**になる。同じフィルタ追加で解消する。

### 1-2. 読み問題で「第2の正しい読み」が誤答として出る

`quiz.js:87-93` の「よみかた」問題。誤答は他の漢字の第1読み（`o.y[0]`）から選ぶが、
それが**出題漢字の第2読みと一致し得る**ことをチェックしていない。

例: 「究」（よみ: きわめる・きゅう）の問題で、「級」の読み「きゅう」が誤答として並ぶ。
「きゅう」を選んだ子は正しいのに不正解になる。

機械検証の結果、**全1026字中256字（約25%）**でこの組み合わせが発生し得る。

**修正案:** 誤答フィルタを `o.y[0] !== e.y[0]` から `!e.y.includes(o.y[0])` に変更（1行）。

---

## 2. 漢字の塔: その他の不具合・改善

### 2-1. HUD の DOM 更新が毎フレーム実行されている（パフォーマンス）

`game.js:236` で `updateHUD()` が `update()` の末尾から呼ばれるため、毎フレーム
（倍速時は1フレームに2回）`querySelector` + `textContent`/`classList` 書き換え +
`refreshPalette()`（パレット全ボタンの走査）が走る。Canvas 描画と違い DOM 書き換えは
スタイル再計算を誘発するので、スマホでの発熱・カクつきの一因になる。

**修正案:** 値が変わったときだけ書く（前回値をキャッシュして比較）か、HUD 更新を
10Hz 程度に間引く。要素参照も初回に取得してキャッシュする。

### 2-2. はやおしボーナスが成績に計上されない

`game.js:287-295` `callWaveEarly()` は `this.coins += bonus` と直接加算しており、
`gainCoins()` を経由しないため `session.coinsEarned`（リザルトの「かせいだコイン」）に
含まれない。**修正案:** `this.gainCoins(bonus, {x: GRID.w/2, y: GRID.h/2})` に統一。

### 2-3. セーブデータのネスト補完が浅い

`save.js:37-40` は `stats` と `settings` だけ深い補完をしているが、`daily` は対象外。
将来 `daily` に新フィールドを足した場合、既存セーブ（同日ロード）で undefined になる。
また `save.js:112` の `this.data.xp += 0;` はデッドコードなので削除してよい。

### 2-4. 復習キューがリロードで消える

`quiz.js:13` の `reviewQueue` はメモリ上のみ。まちがえた漢字（字だけでも）をセーブに
持たせれば、翌日の再挑戦時にも優先出題でき、PLAN の T3（苦手な字だけ復習）の土台になる。
図鑑には既に `zukan[k] = {s, c}` があるので、`s - c`（まちがえた回数）から
「苦手な字」を再構成するだけでも実装できる。

### 2-5. 1・5・6年生は問題タイプが実質3種類だけ

`questions-data-extra.js` の自動生成分（1・5・6年）は漢字カードのみで、
おくりがな・じゅくご・つかいわけ・はんたいご・ぶしゅ・かくすうの各問題が出ない
（データが空なので `build()` が null を返し、よみ/かんじ/いみだけが出る）。
2〜4年との体験差が大きい。PLAN T2（部首・画数付与）と合わせ、まずは 5・6年の
じゅくご・たいぎ語データの追加が学習効果が高い。

### 2-6. 小さい改善

- 図鑑の初期タブが常に「2年生」固定（`screens.js:193`）。`SaveMgr.data.settings.grade`
  （mix のときは "2"）を初期値にすると親切。
- クイズ制限時間 12秒が固定（`quiz.js:10`）。低学年向けに設定でのばせると離脱防止になる。
- ボスウェーブ時、`startWave()` と `spawnEnemy()` の両方でバナー＋咆哮＋シェイクが発火し
  演出が二重になる（`game.js:257-259` と `game.js:265-269`）。

---

## 3. 漢字ドリル（grade1〜6.html + js/kanji-app.js）

### 3-1. 進捗の指標がページ間で食い違う

- トップ `index.html:238-261` は `learnedKanji.length`（一度でも正解した漢字数）
- 各学年ページの「全体進捗」`kanji-app.js:230-234` は `scores` の正解数合計 ÷ 全問題数

後者はカテゴリを再挑戦して前回より悪いと**進捗が下がる**（直近の成績で上書きされるため）。
子どもにとって「進捗」は減らないほうが動機づけになるので、**両方とも learnedKanji 基準に
統一**するのが簡単で分かりやすい。

### 3-2. 学年ごとの定数がトップページにハードコード

`index.html:256-261` に localStorage キーと字数（80/160/200/202/193/191）が直書き。
さらに **2年生だけキーが `kanji_state`**（他は `kanjiN_state`）というレガシー命名が残っており、
今後の事故のもと。キーと字数の対応表を1つの小さな JS（もしくは各 data-gradeN.js が
`window.GRADE_META` に登録する形）に寄せる。※キー自体の改名は既存ユーザーの記録が
消えるため、移行コード（旧キー→新キーのコピー）とセットで。

### 3-3. カテゴリ途中でホームに戻ると採点が全部消える

採点結果は全問回答時にのみ保存（`kanji-app.js:202-206`）。20問超のカテゴリ
（3年生は最大30問前後）で途中離脱すると成果ゼロになる。**修正案:** `markAnswer` の
たびに learnedKanji だけでも `saveState()` する（1行）。scores は完走時のままでよい。

### 3-4. 書き取りキャンバスの解像度・アスペクト比

`kanji-app.js:106` で `width=600 height=120` 固定、CSS 側は `width:100%; height:120px`
（`css/kanji-app.css:104`）。表示幅とビットマップ幅の比率が縦横で異なるため、
ペンの線が縦横で太さが変わって見え、高DPI端末では縦方向がぼやける。
**修正案:** `initCanvas` で `canvas.width = rect.width * devicePixelRatio` のように
実表示サイズ×DPRで確保する（リサイズ時は描画内容が消えてよければ再設定のみ）。

### 3-5. まちがえた問題の復習導線がない

漢字の塔・サバイバーには復習キューが入ったが、ドリル本体には「まちがえた問題だけ
もう一度」がない。リザルト画面に「まちがえた問題をやりなおす」ボタンを置き、
`learnedKanji` に入っていない字の問題だけで `startQuiz` 相当を組むだけで実現できる。
学習効果の観点では本レポート中もっとも費用対効果が高い機能追加（PLAN の S3/T3 と同思想）。

### 3-6. 保守性: 6ページがほぼ同一のコピー

grade1〜6.html は読み込む data ファイル以外ほぼ同一。字数変更のたびに6+1ファイル
（index含む）の修正が要る。`grade.html?g=3` の1枚に統合（またはビルド時生成）を検討。
機能追加（3-5 など）を6回コピーしなくて済むようになる。

### 3-7. 小さい改善

- `switchTab`（`kanji-app.js:40-45`）がボタンの並び順 `i===0/1` に依存していて壊れやすい。
  `data-tab` 属性での特定に。
- Google Fonts の `@import`（各HTML冒頭）はレンダーブロッキング。
  `<link rel="preconnect">` + `<link rel="stylesheet">` へ。
- `user-scalable=no` はアクセシビリティ非推奨（誤操作防止の意図は理解できるので判断次第）。

---

## 4. サイト共通・運用

### 4-1. データ検証の CI 化（おすすめ）

`tools/check-grade-data.mjs` という優れた検証資産が既にあるのに手動実行のみ。
GitHub Actions で push ごとに:

1. ドリル: 全6学年の check-grade-data.mjs
2. 漢字の塔: QDATA の字数（160/200/202…）と、**選択肢重複の検出**（本レポート1-1の
   シミュレーションをそのままテスト化できる）

を回せば、データ修繕時の退行を機械的に防げる。今回の 1-1/1-2 のような
「データとロジックの組み合わせで発生する問題」はレビューでは見つけにくく、テスト向き。

### 4-2. ainews.html

問題なし。エスケープ処理・`rel="noopener"`・テーマ切替とも適切に実装されている。

### 4-3. 学習記録の横断ダッシュボード（PLAN C2）への布石

3-2 でキー・字数のメタ情報を一元化すると、トップページに「3コンテンツ合計で今日
なんもん解いたか」を出す C2 の実装が素直になる。漢字の塔側は `kanjiTD_save_v1` の
`zukan` / `daily.stats` が既に必要な情報を持っている。

---

## 優先順位まとめ

| 優先 | 項目 | 種別 | 規模 |
|---|---|---|---|
| ★★★ | 1-1 選択肢重複で正答が不正解になる（塔） | バグ | 小（フィルタ+ガードで数行） |
| ★★★ | 1-2 第2の読みが誤答扱い（塔） | バグ | 小（1行） |
| ★★ | 3-3 途中離脱で採点消失（ドリル） | バグ寄り | 小（1行） |
| ★★ | 3-1 進捗指標の統一（ドリル/トップ) | UX | 小 |
| ★★ | 3-5 まちがえた問題のやりなおし（ドリル） | 学習効果 | 中 |
| ★★ | 2-1 HUD毎フレーム更新の抑制（塔） | 性能 | 小〜中 |
| ★★ | 4-1 データ検証のCI化 | 運用 | 小 |
| ★ | 2-5 1・5・6年の問題タイプ拡充（塔） | 学習効果 | 大（データ作成） |
| ★ | 3-2 キー/字数の一元化（+2年キーの移行） | 保守性 | 小〜中 |
| ★ | 3-4 キャンバスDPR対応（ドリル） | UX | 小 |
| ★ | 2-2〜2-4, 2-6, 3-6, 3-7 | 各種 | 小 |
